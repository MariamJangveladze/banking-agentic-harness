# Security policy

Security fixes are applied to the latest commit on `main`.

Use GitHub's **Report a vulnerability** link in the Security tab. Do not open
a public issue containing credentials or organizational data.

This is a synthetic portfolio harness, not a core-banking system. Do not expose
the optional API or connect it to real processes without enterprise identity,
role enforcement, throttling, encryption, and monitoring.
